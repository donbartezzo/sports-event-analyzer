---
export const prerender = false;
// Server-side redirect to make /events/list the default landing
const target = "/events/list";
return new Response(null, { status: 308, headers: { Location: target } });
---
